{
  "name": "beetles",
  "private": true,
  "description": "Run scripts from the repo root; app lives in project/",
  "scripts": {
    "dev": "npm run dev --prefix project",
    "build": "npm run build --prefix project",
    "preview": "npm run preview --prefix project",
    "install:app": "npm install --prefix project"
  }
}
